*inicilizar un repositorio en git es
	git init

*Muestra los archivos que no se han agregado al repo
	git status

*Agregar archivos al stech
	git add *.html //agrega todos los html
	git add --all
	git add nombre del archivo

*Agregar los archivos a un repositorio
	git comit -m "Inicializamos el proyecto"

*Muestra las modificaciones 
	git log

*Muestra en donde se iso la modificacion del archivo
 git diff 

 *agrgar a github
 	tener que crear un repositorio en githubcopiar y pegar los comandos que estan en la pagina a la terminal ejemplo
 		git remote add origin https://github.com/AdrianEsteban/Comandos-git-.git
		git push -u origin master